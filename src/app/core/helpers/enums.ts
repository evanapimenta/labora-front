export enum ENUM_PAGES {
  home = 'home',
  user = 'user',
  profile = 'profile',
  exams = 'exams',
  laboratories = 'laboratories',
  management = 'management',
  schedule = 'schedule',
}

export enum ENUM_STATUS {}
